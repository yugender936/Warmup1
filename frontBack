public String frontBack(String str) {
  if(str.length() > 1){
  char first = str.charAt(0);
  char last = str.charAt(str.length()-1);
  String str1 = "";
  str1 = str1 + last + str.substring(1,str.length()-1) + first;
   return str1;
  }
  return str;
}
