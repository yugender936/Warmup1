public String delDel(String str) {
  String str1 = "";
  if(str.length() < 4){
    return str;
  }
  if(str.substring(1,4).equals("del")){
  str1 = str1 + str.charAt(0) + str.substring(4,str.length());
  return str1;
  }
  return str;
}
